export enum IdentificationTypes {
  citizenId = 'citizenId',
  foreignId = 'foreignId',
  passport = 'passport',
  identityCard = 'identityCard',
}

export enum Gender {
  male = 'male',
  female = 'female',
  other = 'other',
}

export enum Status {
  ACTIVE = 'ACTIVE',
  DISABLED = 'DISABLED',
}
